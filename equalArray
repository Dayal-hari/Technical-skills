//Given an integer a of size n in one second u can increase the value of one element find the minimum time in second
//to make all the elements of the array equal
import java.util.*;
public class equalArray{
    public static void main(String[]args){
        int[]arr={2,4,1,3};
        int max=arr[0];
        for(int i=0;i<arr.length;i++){
            if(arr[i]>max){
                max=arr[i];
            }
        }
        int time=0;
        for(int i=0;i<arr.length;i++){
            time+=max-arr[i];
        }
        System.out.println("time:"+time);
    }
}
//to minimize the time make all elements are equal to max element in the array
//find the max element in the array
//for every element calculate howmuch it needs to be increase to reach the maximum
//sum all those diffrences

